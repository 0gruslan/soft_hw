# Отчет по практике: RabbitMQ vs Redis

## 1. Цель

Сравнить RabbitMQ и Redis как брокеры сообщений в одинаковых условиях:
- пропускная способность;
- влияние размера сообщения;
- момент деградации single instance.

## 2. Конфигурация стенда

- ОС: `__________`
- CPU: `__________`
- RAM: `__________`
- Python: `__________`
- RabbitMQ: `3.13-management`
- Redis: `7.2`
- Producers: `_____`
- Consumers: `_____`
- Длительность одного теста: `_____ сек`

## 3. Методика

- Формат сообщения одинаковый для обоих брокеров: JSON (`id`, `ts_ns`, `payload`).
- Сначала baseline, потом тест размера сообщения, потом тест интенсивности.
- Параметры:
  - Размеры: `128 B`, `1 KB`, `10 KB`, `100 KB`
  - Интенсивность: `1000`, `5000`, `10000 msg/sec`

## 4. Результаты (вставьте из CSV)

| broker | scenario | size(B) | rate(msg/s) | sent | consumed | mps | p95(ms) | max backlog | errors | lost | degraded |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| | | | | | | | | | | | |
| | | | | | | | | | | | |
| | | | | | | | | | | | |

## 5. Анализ

### 5.1 Базовое сравнение

- RabbitMQ: `__________`
- Redis: `__________`
- Вывод: `__________`

### 5.2 Влияние размера сообщения

- На маленьких сообщениях лучше: `__________`
- На больших сообщениях быстрее деградирует: `__________`
- Почему (кратко): `__________`

### 5.3 Влияние интенсивности потока

- RabbitMQ начал деградировать при: `__________`
- Redis начал деградировать при: `__________`
- Признаки деградации: `рост backlog / рост p95 / ошибки / потери`

## 6. Выводы

1. Брокер с большей пропускной способностью: `__________`.
2. Брокер, который лучше переносит рост размера сообщения: `__________`.
3. Точка деградации single instance RabbitMQ: `__________`.
4. Точка деградации single instance Redis: `__________`.
5. Какой инструмент лучше для такого сценария и почему: `__________`.

## 7. Скриншоты

Вставьте:
- запуск `docker compose`;
- запуск `benchmark.py`;
- фрагмент `CSV`/`summary.md` с результатами.